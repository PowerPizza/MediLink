import { Text, View } from "react-native";
import { COMMON_STYLES } from "../../commons/common-styles";

export default function ReportsScreen() {
    return (
        <View style={COMMON_STYLES.screenContent}>
            <Text>Welcome to reports!</Text>
        </View>
    );
}